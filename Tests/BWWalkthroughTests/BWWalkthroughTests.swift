import XCTest
@testable import BWWalkthrough

final class BWWalkthroughTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(BWWalkthrough().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
